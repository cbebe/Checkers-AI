#include "check.h"

// determines row offset
bool rowOS(int8 pos) {
  return ((pos % 8) / 4);
}

bp empty::moveU(const Board& board, int8 pos) {
  
}

std::list<mp> pieceMoves(const Board& board, int8 piecePos) {

} 